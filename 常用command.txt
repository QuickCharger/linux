du
	-s, --summarize
		display only a total for each argument
	-h, --human-readable
		print sizes in human readable format (e.g., 1K 234M 2G)

	du -sh	显示当前目录下总的空间占用
	du -sh pwd	显示pwd的空间占用 如果pwd是文件则显示pwd文件的空间 如果pwd是路径则显示pwd下所有文件的空间占用
